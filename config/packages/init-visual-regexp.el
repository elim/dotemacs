;;; init-visual-regexp.el --- A setting of the visual-regexp.
;;; Commentary:
;;; Code:

(global-set-key (kbd "M-%")   'vr/query-replace)
(global-set-key (kbd "C-M-%") 'vr/replace)

(provide 'init-visual-regexp)
;;; init-visual-regexp.el ends here
