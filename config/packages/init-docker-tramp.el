;;; init-docker-tramp.el --- A setting of the docker-tramp.
;;; Commentary:
;;; Code:

(use-package docker-tramp
  :init (set-variable 'docker-tramp-use-names t))

(provide 'init-docker-tramp)
;;; init-docker-tramp.el ends here
